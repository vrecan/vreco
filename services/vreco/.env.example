# Blockening account management (optional)
# If set, enables /games/blockening/account for login and account deletion.
# Get TALO_ACCESS_KEY from the Talo dashboard (same key used by blockening builds).
TALO_ACCESS_KEY=

# Session secret for account cookies (32+ random characters recommended).
# Generate with: openssl rand -base64 32
SESSION_SECRET=
